{
  "name": "todo-webapp",
  "type": "web-application",
  "version": "0.1.0",
  "language": "TypeScript",
  "buildpack": "docker",
  "appPath": "todo-webapp",
  "entrypoint": "deployment/web-application",
  "exposure": "internet",
  "dependencies": [
    {
      "kind": "component",
      "name": "todo-api",
      "description": "Calls the Todo API to list the signed-in user's todos, create a new one, and mark one complete."
    },
    {
      "kind": "platform-resource",
      "name": "user-auth",
      "resourceType": "thunder-app",
      "parameters": { "scopes": "openid profile email" },
      "description": "Thunder-issued OIDC client the SPA uses to sign the user in via PKCE."
    }
  ],
  "description": "React single-page app where a signed-in user manages their own todo list: sign in, add a todo, view the list, and mark a todo complete. Holds no data itself — every read/write goes through todo-api. Does not support editing or deleting todos.",
  "skillsPinned": ["wireframes", "react-webapp", "thunder-authentication"]
}
